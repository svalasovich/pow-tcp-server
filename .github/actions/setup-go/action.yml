---
name: Setup Golang
description: Install required version of golang setup

runs:
  using: composite
  steps:
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version-file: ./go.mod
